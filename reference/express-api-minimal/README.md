# Synaxion Reference — Express API Minimal

`express-api` 프로파일의 **최소 인스턴스**. `GET /health`·env contract·ops 문서만으로 `verify:reference:express`를 통과한다.

```bash
cd synaxion-core
pnpm run verify:reference:express
pnpm run verify:scaffold:express-api
```

**최종 업데이트**: 2026-05-24
