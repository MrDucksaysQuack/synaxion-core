# ENV Contract (Reference — Express API)

| Variable | Required | Purpose |
|----------|----------|---------|
| `PORT` | yes | HTTP listen port |
| `SENTRY_DSN` | no | Error tracking |

Validated in `src/lib/env.mjs`.
