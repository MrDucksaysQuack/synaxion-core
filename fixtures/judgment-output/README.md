# Judgment 출력 샘플 (스키마 검증 전용)

이 디렉터리의 JSON은 **Synaxion JudgmentResult** 형식이며, `docs/constitution/fixtures/*.json` 최상위를 스캔하는 `check:decision-rules`와 분리한다.

- `pnpm run check:judgment-output-schema` — `judgment-output.schema.json` 대비 검증
